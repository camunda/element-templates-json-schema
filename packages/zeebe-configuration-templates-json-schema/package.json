{
  "name": "@camunda/zeebe-configuration-templates-json-schema",
  "version": "0.1.0",
  "description": "JSON Schema for (Zeebe) Configuration Templates",
  "files": [
    "resources"
  ],
  "scripts": {
    "test": "mocha --reporter=spec --recursive test/spec",
    "test:integration": "mocha --reporter=spec --recursive test/integration",
    "dev": "npm run test -- --watch",
    "all": "run-s build test",
    "build": "run-s build:schema",
    "build:schema": "node ../../tasks/generate-schema.js --input=./src/schema.json --output=./resources/schema.json",
    "prepare": "run-s build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/camunda/element-templates-json-schema.git",
    "directory": "packages/zeebe-configuration-templates-json-schema"
  },
  "keywords": [
    "json-schema",
    "configuration-templates",
    "element-templates",
    "camunda"
  ],
  "license": "MIT"
}
